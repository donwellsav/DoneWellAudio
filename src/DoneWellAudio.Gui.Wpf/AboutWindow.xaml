<Window x:Class="DoneWellAudio.Gui.Wpf.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About DoneWell Audio" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="DoneWell Audio" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AppAccentBrush}" HorizontalAlignment="Center"/>
        <TextBlock x:Name="VersionText" Grid.Row="1" Text="Version 1.0.0" FontSize="16" Foreground="{StaticResource AppForegroundBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <TextBlock Grid.Row="2" Text="Copyright © 2024 DoneWell Audio" FontSize="14" Foreground="{StaticResource AppForegroundBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button Grid.Row="3" Content="Close" Width="80" Click="Close_Click" HorizontalAlignment="Right" Background="{StaticResource AppPanelBrush}" Foreground="{StaticResource AppForegroundBrush}"/>
    </Grid>
</Window>
